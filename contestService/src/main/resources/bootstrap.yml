spring:
  application:
    name: contest-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: false
      retry:
        max-attempts: 20
        initial-interval: 1500
        multiplier: 1.5
        max-interval: 5000
    import-check:
      enabled: false 